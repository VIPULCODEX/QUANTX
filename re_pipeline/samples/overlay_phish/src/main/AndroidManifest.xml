<?xml version="1.0" encoding="utf-8"?>
<!--
  QuantX agentic-RE research sample — lab-only TTP reproduction.
  Reproduces the overlay-phishing / tapjacking primitive used by the
  Anatsa/Octo/Cerberus/Xenomorph banking-trojan lineage — the same technique
  the shipped app's OVERLAY_HOLDER_UNTRUSTED finding targets. See
  ../../../ttp.json for ground truth and README.md at the repo root for scope
  and safety notes.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:icon="@android:drawable/ic_dialog_alert">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".PhishOverlayService"
            android:exported="false" />

    </application>
</manifest>

<?xml version="1.0" encoding="utf-8"?>
<!--
  QuantX agentic-RE research sample — lab-only TTP reproduction.
  Reproduces the accessibility screen-reading primitive used by the
  Anatsa/Octo/Cerberus/Xenomorph banking-trojan lineage. See ../../../ttp.json
  for the recorded ground truth and README.md at the repo root for scope and
  safety notes. Not for distribution; never shipped inside cyber_intel_app.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:icon="@android:drawable/ic_dialog_alert">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".ScreenReaderService"
            android:exported="false"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

    </application>
</manifest>
